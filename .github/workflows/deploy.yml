name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Build Native Rollups
        working-directory: experiments/native-rollups
        run: |
          bun install
          bun run build

      - name: Build EOF
        working-directory: experiments/eof
        run: |
          bun install
          bun run build

      - name: Assemble site
        run: |
          mkdir -p _site/native-rollups _site/eof
          cp index.html _site/
          cp -r experiments/native-rollups/build/* _site/native-rollups/
          cp -r experiments/eof/build/* _site/eof/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
